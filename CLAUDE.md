# CLAUDE.md

気象庁防災情報 XML から JSON API を自動生成するシステム。

## このシステムの最大の設計課題

**取り込みは全国全件、通知は厳しく絞る。** ここを取り違えると失敗する。

気象警報・注意報は府県予報区ごと（全国で約 140 区）に発表され、荒天時はそれぞれが
短時間に何度も更新される。地震も 1 回の揺れで震度速報・震源に関する情報・震源震度情報と
複数の電文が連続する。全件を素通しで通知すると、悪天候の日には数百通が並び、
**通知が多すぎて誰も見なくなる**。これが本システムで最も現実的な失敗モードである。

したがって、通知量を減らすための以下 3 つの仕組みは、あとから足す機能ではなく
最初から設計に組み込む。

### 1. イベント通知ではなく状態差分

気象警報・注意報の電文は差分ではなく、**その府県予報区の現在の全状態**が毎回入っている。
素直に処理すると「変化のない再送」まで通知してしまう。

府県予報区ごとの現在状態（発表中の警報・注意報の集合）を管理し、
前回状態と比較して **新規発表 / 格上げ / 格下げ / 解除** が起きたときだけ JSON に含める。

### 2. 深刻度による分類

| 段階 | 対象 | 扱い |
| --- | --- | --- |
| 即時・個別 | 特別警報、津波警報・注意報、噴火警報、震度 5 弱以上、レベル 4 の各危険警報 | immediate に分類 |
| 集約 | 警報クラス（レベル 3 相当、暴風・波浪・大雪等の警報） | digest に分類 |
| 記録のみ | 注意報、定時情報 | record に分類 |

段階の境界は運用しながら調整する。閾値は `src/config/severity.json` に外出しし、コードに埋めない。

### 3. EventID による束ね

地震の続報は `Head/EventID` が同一。1 事象＝1 JSON オブジェクトとして扱い、続報は
`serialCount` を増やして反映する。新規エントリを積まない。

## 絶対に守る制約

### 気象庁サーバーへのアクセス量

1 日 10GB 以上のダウンロードが検知されるとIPアドレスが遮断される。全国運用でも電文の総量は
1 日数百 MB 程度に収まる見込みだが、以下は交渉の余地のない要件とする。

- フィード取得は必ず条件付き GET（`If-None-Match` / `If-Modified-Since`）を行い、
  304 Not Modified で早期リターンする
- **一度取得した電文 URL は二度取得しない。** 重複排除を通過したものだけが本体を取りに行く
- ポーリング間隔は最短 1 分。それより短くしない
- 電文取得の同時接続数は 5 程度に制限する（並列化しても気象庁側に殺到させない）
- 開発・テスト中に実サーバーへ繰り返しアクセスしない（フィクスチャを使う）

### 学習データ上の情報名を信用しない

2026 年 5 月 29 日に防災気象情報の体系が大きく変わり、大雨・土砂災害・河川氾濫・高潮の情報名が
「レベル 3 大雨警報」「レベル 4 土砂災害危険警報」「レベル 5 土砂災害特別警報」等に変更され、
集約通報や気象防災速報が追加された。暴風・波浪・大雪などは従来名のまま。
ネット上のサンプルコードおよびモデルの記憶は旧体系のものが大半である。

**情報名・電文コードをリテラルで書く場合は、必ず実物のフィード／技術資料で裏を取ること。**
裏が取れないものは推測で書かず、TODO コメントを残して確認を求める。

参照先:
- 情報の取得方法: https://xml.kishou.go.jp/xmlpull.html
- 技術資料: https://xml.kishou.go.jp/tec_material.html
- 電文一覧: https://xml.kishou.go.jp/xmllist.pdf
- 新体系: https://www.jma.go.jp/jma/kishou/know/bosai/keiho-update2026/

## アーキテクチャ

```
GitHub Actions (毎分)
    ↓
poller (src/poller/index.js)
  - フィード条件付き GET
  - 新着エントリ検出
  - 重複排除
    ↓
processor (src/processor/index.js)
  - 電文 XML 取得
  - パース
  - 状態差分計算
  - 深刻度分類
    ↓
api/latest.json に出力
    ↓
archive/{YYYYMM}/{timestamp}.json にアーカイブ
    ↓
GitHub Pages / REST API で公開
```

- **poller**: フィードの条件付き GET → 新着 entry の検出 → 重複排除。
  全国運用では 1 分間に数十件の電文が流れうるため、poller 内で電文本体を直列取得すると
  タイムアウトする。**poller は電文本体を取りに行かない。**
- **processor**: 電文取得 → パース → 状態差分 → JSON に変換。
  GitHub Actions 内で順次実行。気象庁への同時接続は 5 程度に制限する

## 実装の落とし穴（既知）

- **entry の `id` は電文 XML の URL そのもの。** 重複排除キーとしてそのまま使える
- **時刻表現が混在。** フィード全体の `updated` は `+09:00`、各 entry の `updated` は
  `Z`（UTC）。文字列比較すると 9 時間ずれる。必ず Date に正規化して比較する
- **ETag の保存は全エントリの publish 完了後。** 先に保存すると、処理中の例外で
  次回 304 が返り、そのぶんを恒久的に取りこぼす
- **`Head/InfoType` に 発表 / 訂正 / 取消 / 遅延 が入る。** 取消は状態差分の「解除」として
  扱う。無視すると解除済みの警報が出っぱなしに見える
- **`fast-xml-parser` は要素が 1 件のとき配列にしない。** entry も Area も必ず配列に正規化する
- **フィードの保持は約 10 分。** 毎分ポーリングなら約 10 回ぶんの猶予がある。
  数分の障害は次回実行で自然に回復する前提でよく、複雑なリトライは不要

## API 出力形式

最新の JSON は `api/latest.json` で取得可能：

```json
{
  "timestamp": "2026-07-29T10:00:00.000Z",
  "immediate": [
    {
      "timestamp": "2026-07-29T09:58:00.000Z",
      "reportId": "http://example.com/xml/20260729095800_0.xml",
      "reportTitle": "津波警報",
      "infoType": "発表",
      "eventID": "e20260729095800",
      "serialCount": 1,
      "changes": { "total": 1, "added": 1, "upgraded": 0, "downgraded": 0, "removed": 0 },
      "areas": ["北海道", "青森県"]
    }
  ],
  "digest": [],
  "record": [],
  "summary": {
    "total": 1,
    "byLevel": { "immediate": 1, "digest": 0, "record": 0 }
  }
}
```

各利用者システム（[emergency-alert](https://github.com/kwaka1208/emergency-alert) 等）はこの JSON を参照し、
必要に応じてフィルタリング・加工・通知を行う。

## テスト方針

**実サーバーへアクセスするテストは書かない。** `test/fixtures/` に実データを保存して使う。

- 各フィードの Atom XML
- 電文: 気象警報・注意報（複数府県）、同一府県の連続する 2 電文（状態差分テスト用）、
  震源・震度に関する情報（同一 EventID の続報を含む）、津波、特別警報、取消電文

## コーディング方針

- 副作用（fetch / ファイル I/O）と純粋なロジック（パース / 状態差分 / 深刻度判定）を分ける
- 状態差分と深刻度判定は必ず純粋関数にする
- 閾値・情報名の分類は設定ファイルに切り出し、運用しながら調整できるようにする
- 環境変数はエントリポイントで集約し、各モジュールが `process.env` を直接読まない

## 利用例

[emergency-alert](https://github.com/kwaka1208/emergency-alert) が本 API の利用者です。
以下のフローで Slack に通知を送信します：

1. jma-api が毎分 `api/latest.json` を更新
2. emergency-alert が 5 分ごとに JSON を参照
3. `immediate` から未送信エントリをフィルタリング
4. Slack webhook に POST
5. `api/sent-notifs.json` に記録

他の利用者システムも同じ JSON を活用できます（LINE、メール、カスタム通知等）。
